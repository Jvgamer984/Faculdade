var cliques = 0;
function incrementar() {
// Incrementa o valor da variável "cliques"
cliques++;
// Atualiza o valor exibido no elemento com id "contador"
document.getElementById("contador").innerHTML = cliques;
}